# Recommendation model artifacts

Generated recommendation models are stored in versioned directories here and ignored by version
control. Each directory must contain a native model file, feature schema, validation-only tuning
record and checksummed manifest.

Synthetic prototype artifacts must never be represented as evidence of production effectiveness.
They may be promoted toward application integration only after their manifest, safety boundary and
offline evaluation have been reviewed.
